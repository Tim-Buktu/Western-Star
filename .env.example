# Production Environment Variables
# Copy this to .env and update with your values

# Database Configuration
DB_PASSWORD=your-secure-database-password-here

# Frontend URL (where your app will be accessible)
# - Dev (vite): http://localhost:5173
# - Prod (nginx): http://localhost:8080
FRONTEND_URL=http://localhost:8080

# Frontend uses a relative base to hit the API via proxy
# This avoids CORS in both dev (Vite proxy) and prod (Nginx)
VITE_API_URL=/api

# Optional: override Vite dev proxy backend target
# VITE_PROXY_TARGET=http://backend:3001

# Optional: Custom domain settings
# FRONTEND_URL=https://your-domain.com
